import PokeForm
    from "../components/pokeform/PokeForm";
import {
    inputs
} from "../fixtures/inputs";

export default function PokedexPokemonAddPage() {
    return <PokeForm inputs={inputs} />;
}