// import {
//     QueryClient
// } from "@tanstack/react-query";
//
// export const queryClient = new QueryClient();
// queryClient.getQueryCache().subscribe((event) => {
//     if((event.type == "updated" || event.type == "added") && event.action.type === "success" && event.query.queryKey[0] === "pokemons"){
//         // document.getElementById("headline").innerHTML = event.action.data.count - 1;
//     }
// });